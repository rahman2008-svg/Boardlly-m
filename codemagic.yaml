workflows:
android:
environment:
java: 17

scripts:
  - chmod +x gradlew
  - ./gradlew clean
  - ./gradlew assembleRelease
  - ./gradlew bundleRelease

artifacts:
  - app/build/outputs/**/*.apk
  - app/build/outputs/**/*.aab
