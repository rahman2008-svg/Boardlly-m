workflows:
  android-workflow:
    name: Android Workflow

    environment:
      java: 17

    scripts:
      - chmod +x gradlew
      - ./gradlew assembleRelease

    artifacts:
      - app/build/outputs/**/*.apk
      - app/build/outputs/**/*.aab
